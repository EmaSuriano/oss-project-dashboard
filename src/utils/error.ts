import { PROJECT_FILE_NAME } from './constant';

export const GIST_NOT_FOUND_ERROR = `No "${PROJECT_FILE_NAME}" file found inside your Github Gists.`;

export const PARSING_GIST_ERROR = `There was a problem while parsing "${PROJECT_FILE_NAME}" content ...`;

export const INVALID_SETTINGS_ERROR = `Invalid Settings inside "${PROJECT_FILE_NAME}" file ...`;

export const INVALID_PROJECTS_ERROR = `There was a problem while parsing your Projects ...`;
